spring:
    datasource:
        url: jdbc:postgreesql://${PGHOST}:${PGPORT}/%{PFDATABASE}
        username: ${PGUSER}
        password: ${PGPASSWORD}
    jpa:
        open-in-view: false    
        hibernate: 
            ddl-auto: validate